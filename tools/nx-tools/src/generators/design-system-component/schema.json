{
  "$schema": "https://json-schema.org/schema",
  "$id": "DesignSystemComponent",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The component name to be generated",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the name of the component?"
    },
    "componentPartOf": {
      "type": "string",
      "description": "The parent component directory name (only if it is a part)",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "What is the parent component directory name? (only if it is a part)"
    }
  },
  "required": ["name"]
}
