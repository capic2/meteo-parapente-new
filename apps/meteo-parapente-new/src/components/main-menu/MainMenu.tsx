import { AppMenu, AppMenuContent, AppMenuHeader } from '@meteo-parapente-new/design-system';

const MainMenu = () => {
  return (
    <AppMenu>
      <AppMenuHeader></AppMenuHeader>
      <AppMenuContent>

      </AppMenuContent>
    </AppMenu>
  );
};

export default MainMenu;
